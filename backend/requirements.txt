# FastAPI and ASGI
fastapi==0.135.4
uvicorn[standard]==0.34.0
python-multipart==0.0.32

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1

# Configuration
pydantic-settings==2.7.1
pydantic[email]==2.10.5

# Authentication & Security
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Redis
redis==5.2.1

# Background jobs (Redis-backed, asyncio-native)
arq==0.26.3

# Rate Limiting
slowapi==0.1.9

# PDF Generation
weasyprint==69.0

# Barcode & QR
python-barcode==0.15.1
qrcode[pil]==8.0

# HTTP Client (for testing & integrations)
httpx==0.28.1

# Error tracking (optional at runtime; disabled unless configured)
sentry-sdk==2.19.2

# Testing
pytest==9.0.3
pytest-asyncio==0.26.0
hypothesis==6.122.3
aiosqlite==0.20.0
# Used by the deployment-configuration contract tests
PyYAML==6.0.2

# Utilities
python-dateutil==2.9.0.post0
